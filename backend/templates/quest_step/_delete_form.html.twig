<form method="post" action="{{ path('app_quest_step_delete', {'id': quest_step.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quest_step.id) }}">
    <button class="btn">Delete</button>
</form>
