{% extends 'base.html.twig' %}

{% block title %}QuestStep{% endblock %}

{% block body %}
    <h1>QuestStep</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quest_step.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ quest_step.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ quest_step.description }}</td>
            </tr>
            <tr>
                <th>StepOrder</th>
                <td>{{ quest_step.stepOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quest_step_index') }}">back to list</a>

    <a href="{{ path('app_quest_step_edit', {'id': quest_step.id}) }}">edit</a>

    {{ include('quest_step/_delete_form.html.twig') }}
{% endblock %}
