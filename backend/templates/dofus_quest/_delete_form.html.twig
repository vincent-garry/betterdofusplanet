<form method="post" action="{{ path('app_dofus_quest_delete', {'id': dofus_quest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dofus_quest.id) }}">
    <button class="btn">Delete</button>
</form>
