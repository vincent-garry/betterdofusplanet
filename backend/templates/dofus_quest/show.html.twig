{% extends 'base.html.twig' %}

{% block title %}DofusQuest{% endblock %}

{% block body %}
    <h1>DofusQuest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dofus_quest.id }}</td>
            </tr>
            <tr>
                <th>QuestOrder</th>
                <td>{{ dofus_quest.questOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dofus_quest_index') }}">back to list</a>

    <a href="{{ path('app_dofus_quest_edit', {'id': dofus_quest.id}) }}">edit</a>

    {{ include('dofus_quest/_delete_form.html.twig') }}
{% endblock %}
