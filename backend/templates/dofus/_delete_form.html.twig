<form method="post" action="{{ path('app_dofus_delete', {'id': dofu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dofu.id) }}">
    <button class="btn">Delete</button>
</form>
