{% extends 'base.html.twig' %}

{% block title %}Dofus{% endblock %}

{% block body %}
    <h1>Dofus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dofu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dofu.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ dofu.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dofu.description }}</td>
            </tr>
            <tr>
                <th>QuestOrder</th>
                <td>{{ dofu.questOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dofus_index') }}">back to list</a>

    <a href="{{ path('app_dofus_edit', {'id': dofu.id}) }}">edit</a>

    {{ include('dofus/_delete_form.html.twig') }}
{% endblock %}
